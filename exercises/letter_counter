import string
lower_count = 0
upper_count = 0
phrase = str(input("Enter a phrase: "))
for char in phrase:
    if char in string.ascii_lowercase:
        lower_count += 1
    elif char in string.ascii_uppercase:
        upper_count += 1
print("Your phrase has {} uppercase and {} lowercase letters".format(upper_count, lower_count))
